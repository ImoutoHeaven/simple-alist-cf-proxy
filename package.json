{
  "name": "simple-alist-cf-proxy",
  "version": "1.0.0",
  "description": "AList download proxy worker with signature verification and link caching",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "npm run test:vitest && npm run test:node",
    "test:vitest": "vitest run init.sql.test.js",
    "test:node": "node --test tests/*.test.mjs",
    "test:watch": "vitest init.sql.test.js"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "alist",
    "download",
    "proxy"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.23.0",
    "vitest": "^4.0.18",
    "wrangler": "^3.0.0"
  }
}
